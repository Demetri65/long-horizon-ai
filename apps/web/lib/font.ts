import { GeistSans } from "geist/font/sans";
import { GeistMono } from "geist/font/mono";

// GeistSans and GeistMono are NextFontWithVariable instances.
// You can access the CSS variable via the `variable` property.
export const fontSans = GeistSans; // default sans font
export const fontMono = GeistMono; // monospaced font